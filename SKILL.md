---
name: kaoyan-material-organizer
description: 将考研教材、课件、讲义、截图、OCR、PDF 和学习记录整理为本地可追溯知识库，并通过老师式自然语言交互提供带来源检索、问答、复习、学习建议和基于教材原页的可视化解释路由。适用于 Windows、Obsidian 和中文考研资料；当用户希望直接描述学习目标或卡点，由技能自动定位本地教材、选择高效工作流、按书中代码或变量讲解、制作交互演示，或决定是否沉淀学习记录时使用。默认优先复用现有入口和即时判断，避免冗余数据库、重复能力和无必要持久化。
---

# Kaoyan Material Organizer

## 使用目标

帮助使用者把本地考研资料整理进以下稳定主链：

```text
原始来源 -> 证据 -> 考纲 -> claim -> canonical card -> query -> learner
```

所有机器路径、资料目录和运行策略都从使用者自己的 `kaoyan.config.json` 读取。不要假设仓库自带个人资料、教材 profile 或正式考纲定义。

## 首次使用

1. 将 `kaoyan.config.example.json` 复制为 `kaoyan.config.json`。
2. 配置本机的 `vault_root`、`kb_root`、`backup_root` 和 Python 路径。
3. 运行 `scripts/kb.py doctor` 检查环境。
4. 根据实际资料选择 `book`、`sync`、`query`、`ask` 或 `learner` 工作流。

仓库中的 Markdown 和 JSON 使用 UTF-8。在 PowerShell 中读取中文时显式使用 `Get-Content -Encoding utf8`；若显示异常，先运行 `scripts/kb.py doctor` 查看终端编码提示。

## 稳定入口

默认只通过 `scripts/kb.py` 调用功能：

- `doctor`：检查路径、Python、OCR 和终端编码。
- `book`：接入纸质书、PDF、页码映射、OCR、复核与分类。
- `sync`：同步证据、考纲、知识卡和学习层。
- `query`、`ask`：本地检索与带来源问答。
- `review`：处理证据、冲突和 refinement 队列。
- `learner`：生成学习记录、复习卡和辅导上下文。
- `maintain`：执行受控维护。
- `snapshot`、`run`、`migrate-vault`：快照、可恢复运行和资料库迁移。

底层脚本属于实现细节；只有在 `kb.py --help` 无对应入口时，才直接调用 README 明确列出的配置脚本。

## 教学交互原则

- 接受自然语言学习目标和模糊描述，从当前任务、学科、章节、卡点与期望输出中推断意图，自动选择最短的稳定入口和必要技能；不要要求使用者先学习命令或技能名称。
- 先判断使用者卡在题意、概念、步骤、指针变化还是表达，再沿其已接受的心智模型给出最短可理解路线；可用时读取受限的 learner 或 teaching context，但不得跨学科泄漏或让历史压过当前问题。
- 只有缺失信息会实质改变资料来源、解法或学习产物时才提问。其余情况先完成可逆的检索与解释，并把工具细节留在后台。
- 像老师一样先讲清关键联系，再用例题、图示或一个小检查确认理解；避免一次堆砌多个平行方案。最终回答优先呈现结论、理解抓手和下一步练习。

## 轻量与去冗余原则

- 老师式路由默认由本技能规则、当前任务和受限 teaching context 即时完成，不建设持久化路由数据库，也不默认保存完整对话、逐轮决策或工具轨迹。
- 开发前先盘点 `scripts/kb.py`、现有脚本、schema、learner 与 teaching context；能够组合现有能力解决时，不新增平行技能、CLI 入口、数据表或同义产物。
- 只有同类误判反复出现，或正确性、安全、恢复需要机器检查时，才新增确定性代码。优先增加纯函数、小模块、聚焦测试或 schema，并继续隐藏在 `scripts/kb.py` 后面。
- 每类事实和契约只保留一个权威来源；复用现有 schema 与公共 helper。派生的 JSON、Markdown 和可视化只是视图，不得形成第二套真相。
- 持久化限于原始来源与证据、使用者明确保存的问答或 learner event、确认过且有范围和过期规则的教学偏好，以及必要的恢复产物。临时可视化、候选路由和中间提示默认不入库。
- 新增持久化产物前必须明确消费者、失效或重建方式、保留期限和空间上界；任何一项不明确时，保持即时计算或临时输出。

## 本地教材解释与可视化

当使用者要求“按书上答案讲解”“书上有吗”“书上怎么推”“原文是什么”“从某章找原题”或“画图解释算法”时，按以下顺序处理：

1. 若当前学习工作区存在当前任务、学科锚点或章节索引，先读取这些入口，再检索历史记录。
2. 优先使用 `scripts/kb.py query`，并尽量提供 `--subject`、`--chapter`、`--book-title` 或 `--printed-page`；出现 `P49`、`第49页`、`例2.29` 等页码或题号信号时，必须检查返回的 `page_anchor.match_status`。
3. 显式页码属于硬约束：只有 `exact_evidence` 或 `exact_asset` 才算定位成功；`ambiguous`、`unmapped`、`not_found` 时不得用其他页的语义命中冒充。索引未命中时继续检查正式页码映射和配置登记的原图目录，正式资料仍无法定位后才能要求使用者重新拍照。
4. 在制作解释前确认书名或资料名、章节、印刷页码、题号和所选解法。存在多个解法时不得混合，选择不明确且会改变演示时再询问使用者。
5. 需要展示指针、状态或步骤变化时调用可用的交互可视化技能；只需要教材风格静态插图时调用图像生成技能。不要要求使用者记住技能名称，自然语言意图足以路由。
6. 保留原书变量名、代码顺序和结构条件；明确区分原始证据、书中结论与新增的解释层。

教材事实核验必须先走 `kb.py ask` 或 `kb.py query` 的结构化结果。没有正式 claim/evidence/OCR 时，只能说明“当前本地识别结果未确认”，不得把章节摘要、页码映射或补充讲解说成书上原文。已定位原图但无结构化正文时，默认停在未确认；只有使用者继续明确要求后才人工阅图，并标注为“人工阅图核对”，不可冒充 OCR 引用。

交互演示至少验证初始状态、一个中间状态和最终状态。算法类演示同时显示当前代码、关键变量或指针、结构变化、不变量、时间复杂度和空间复杂度；空结构或单元素会改变行为时，再加入边界状态。

可视化文件默认只是临时解释产物，不自动成为 evidence、claim、知识卡或学习记录。只有使用者明确要求保存时，才通过 `ask --save` 或合适的 `learner` 工作流沉淀，并保留资料来源、页码和解法标识。

## 会话与学习续接

- 长对话开始时，先读取当前任务和对应学科专题锚点；它们决定真实停点，历史会话只能补充，不得覆盖。
- 教材讲解固定按“页码/证据状态 -> 原题范围 -> 补充同类推导 -> 一个可选自测”组织。补充推导必须明确标注，且不得继承原题的页码、题号或教材原文身份。
- “讲解”默认零写入。只有使用者明确说“记录”或“同步”时，才生成会话蒸馏候选并经显式确认发布；候选必须包含原题/补充内容关系、来源类型、掌握状态、下次续接和可选自测。
- 学习者反馈可记录为“已讲解、跟随完成、独立写过、待验证”，但它只能改善后续教学呈现，绝不升级教材事实或 OCR 证据。

## 资料处理边界

纸质教材按以下顺序处理：

```text
inspect -> map-pages -> OCR -> review -> classify
```

- 原图是只读可信源，不通过移动文件表达书籍或章节归属。
- OCR 输出在完成审核和证据门控前，不直接写入正式 evidence 或 claim。
- OCR 置信度不等于章节分类置信度。
- 人工文件、人工审核结果和人工考纲不得自动覆盖。
- 删除、迁移、批量发布等操作必须先预览范围，并保留受控恢复路径。

## 隐私规则

- 默认只处理使用者明确指定的本地资料目录。
- 远程 OCR 默认关闭；未经使用者明确允许，不向外部服务发送资料。
- API key 只从进程环境读取，不写入配置、日志或学习资料。
- 本地配置、知识库、快照、教材 profile 和考纲定义不得提交回公共仓库。
